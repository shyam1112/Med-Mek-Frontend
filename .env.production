# Loaded automatically by `npm run build` (CRA convention). Safe to commit —
# this is just the public API endpoint, not a secret. Update this if the
# backend ever moves to a different host/path.
REACT_APP_API_URL=https://api.thecartnova.com/medmek-api
